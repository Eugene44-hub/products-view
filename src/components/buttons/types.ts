export interface IButtonThemes {
  base: string;
  size: {
    fullWidth: string;
    sm: string;
    md: string;
    base: string;
  };
  variant: {
    primary: string;
    outlined: string;
    disabled: string;
    error: string;
    disabledOutline: string;
  };
}
